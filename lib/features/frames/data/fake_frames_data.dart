import 'package:snapframe/features/frames/data/sample_frame.dart';
import 'package:snapframe/features/frames/domain/frame.dart';

/// The frames Browse shows until Storage (Milestone 3+) or the frame editor
/// (Milestone 5) exist: one free and one Pro, each with its own bundled
/// overlay art. Both are official, so the Community tab is empty for now.
final List<Frame> fakeFrames = [sampleFrame, photoDumpFrame];
