/// Stable filter identifiers — kept separate from their display labels
/// (which are localized in the view) so a translated label can never
/// desync from the filtering logic.
enum BrowseFilter { newest, popular, free, pro }
